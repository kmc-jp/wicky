%h2 プロジェクト一覧
%section
  - projects.each do |proj|
    %h3.project
      %a{ href: url("/projects/#{proj.id}") } #{proj.name}
%h2 新規プロジェクトの作成
%section
  %form{ action: url('/projects/!add'), method: :post, name: 'add-project' }
    %p
      %input{ name: :name }
    %p
      %button{ type: :submit } プロジェクトを作成する

